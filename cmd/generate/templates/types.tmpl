package {{ .Package }}

import "{{ .Module }}/internal/shared/models"

type {{ .Domain}}Request struct {
    // struct for request goes here!
}

type {{ .Domain }}Response struct {
    // struct for reponse goes here!
}

func (req {{ .Domain }}Request) ToModel(item *models.{{ .Domain }}) *models.{{ .Domain }} {
    return item
}